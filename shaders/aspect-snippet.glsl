  // vec2 ratio = vec2(u_res.x / u_res.y, 1.0);
  // float ratio = u_res.x / u_res.y;
  // vec2 uv = vec2(gl_FragCoord.xy / u_res.y) - vec2(ratio/2.0, 0.5);
  
  // vec2 uv = gl_FragCoord.xy / u_res.xy;
  //= vec2(fragCoord.xy/iResolution.y - vec2(ratio/2.0, 0.5), zNear);

 // vec3 	viewportCoord 	= 
 //vec3(fragCoord.xy/iResolution.y - vec2(ratio/2.0, 0.5), zNear);
 
  // uv.xy *= 2.0;
  // uv.xy -= 1.0;
  // uv *= aspectRatio;